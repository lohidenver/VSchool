import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <div>
    <h1> Hello fff World </h1> <p> This is the new way to do things! </p>{" "}
  </div>,

  document.getElementById("root")
);
